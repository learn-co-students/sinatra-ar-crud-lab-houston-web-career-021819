<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Edit the Article</title>
  </head>
  <body>

    <form action="/articles/<%= @article.id %>" method="POST">
    <input type="hidden" name="_method" value="PATCH" />
      <p><label>Title: </label><input type="text" name="title" value="<%= @article.title %>" /></p>
      <p><label>Content: </label><input type="text" name="content" value="<%= @article.content %>" /></p>

      <input type="submit" value="Confirm Editing">
    </form>
  </body>
</html>
